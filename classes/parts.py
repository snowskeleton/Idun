#from classes.device import Device

class Parts:
    name = str
    cost = int #probably going to store number of pennies and divide by 100 when presenting to users.
    #compatibleWith = [Device.modelNumber]
    notes = [str] #not sure what I'm going to use this for, but I'm thinking it might be helpful to comment little tidbits here